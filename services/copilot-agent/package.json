{
  "name": "copilot-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Aladin copilot agent sidecar: runs the Claude Agent SDK per copilot turn, consuming tools from the Go MCP server (:8090) and streaming NDJSON events back to the Go API, which republishes them to the dock over the realtime hub.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.218",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@openai/agents": "^0.17.0",
    "express": "^4.21.2",
    "zod": "^4.4.3"
  }
}
